# The macOS Build Environment
os: osx

# Homebrew dependencies
addons:
  homebrew:
    packages:
    - pipenv
    update: true

# command to install dependencies
install:
  - pipenv install --dev

# command to run tests
script:
  - macdaily --help
