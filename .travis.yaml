dist: trusty
sudo: false

os: osx

language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
  - "pypy3.5"

addons:
  homebrew:
    packages:
    - pipenv
    update: true

# command to install dependencies
install:
  - pipenv install --dev

# command to run tests
script:
  - macdaily --help
